# Required. Get yours at https://pinboard.in/settings/password
PINBOARD_API_TOKEN=

# Optional. Bitly's v4 expand endpoint rejects unauthenticated requests, so
# without this bit.ly bookmarks are left unexpanded (never deleted).
# Create one at https://app.bitly.com/settings/api/
BITLY_ACCESS_TOKEN=
